// aritmatika
// + -> penjumlahan = a + b
// - -> pengurangan = a - b
// * -> perkalian = a * b
// / -> pembagian = a / b
// % -> modulus = a % b

// relasional -> membandingkan 2 nilai
// == -> sama dengan -> a == b
// != -> tidak sama dengan -> a != b
// > -> lebih besar -> a > b
// < -> lebih kecil -> a < b
// >= -> lebih besar atau sama dengan -> a >= b
// <= -> lebih kecil atau sama dengan -> a <= b

// operator logika
// digunakan untuk operasi logika pada boolean
// && -> AND (logikan dan) a && b
// !-> (logika tidak) NOT -> !a
// || (logika or) a || b

// operator bitwise
// & -> and bitwise -> a & b
// | -> or bitwsise -> a | b
// ^ -> xor bitwise -> a ^ b
// ~ -> NOT bitwise -> ~a
// << -> shift kiri bitwsise -> a << 1
// >> -> shift kanan bitwsie -> a >> 1

// operator assign
// = -> penugasan -> a = b
// += -> tambah assign a += b
// -= -> kurang assign a -= b
// *=
// /=
// %=

#include <iostream>

int main() {
  20 / 3
  return 0;
}
